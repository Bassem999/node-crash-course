<!DOCTYPE html>
html(lang="en")
    include ./partials/head

    body
        include ./partials/header
        .container
            .row
                .col-md
                    each blog in blogs
                        h1(class="my-4") Welcome To Bassem's #{title}
                        // Blog Post
                        div(class="card mb-4")
                            a(href=`/posts/${blog._id}`)
                                img( class="card-img-top" src="http://placehold.it/750x300" alt="Card image cap")
                            .card-body
                                h2.card-title #{blog.title}
                                p.card-text #{blog.body}
                                a(href=`/posts/${blog._id}` class="btn btn-primary") Read More →
                            div(class="card-footer text-muted")  Posted on #{blog.createdAt}, by #{blog.author}

                    else
                        h1 No posts
        form( id='uploadForm' action='http://localhost:3000/upload' method='post' encType="multipart/form-data")
            input(type="file" name="sampleFile")
            input(type='submit' value='Upload!')
    script(src="https://stackpath.bootstrapcdn.com/bootstrap/5.0.0-alpha2/js/bootstrap.bundle.min.js" integrity="sha384-BOsAfwzjNJHrJ8cZidOg56tcQWfp6y72vEJ8xQ9w6Quywb24iOsW913URv1IS4GD" crossorigin="anonymous")



